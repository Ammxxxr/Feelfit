Day 13  README Update

Task: Bring the project's README up to date — it was still describing an early, incomplete version of the app.
Work completed:

Rewrote README.md to accurately reflect all features built through Day 12: exercise picking with category filters, optional workout naming, set logging with input validation, the workout timer, history with delete, and the stats screen
Documented the tech stack and rationale (Expo, no external navigation library, AsyncStorage-only persistence)
Added clear run instructions, including the web preview fallback (w in the terminal) for testing without a physical device
Documented the project's file structure and the workout data model (with an example JSON record)
Added a "Known limitations / not yet built" section for transparency about what the app intentionally doesn't do yet (accounts, cloud sync, custom exercises, notifications, tests)

Decisions made:

Treated this as a documentation only day with no app logic changes, since live device testing wasn't available a good fit for keeping project momentum without needing to verify UI behavior

Issues encountered & resolved:

None straightforward content update.

Files touched: README.md